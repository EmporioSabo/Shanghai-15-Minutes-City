# ── Raw data (too large for git) ─────────────────────────────────
POI 2024/
urban data/

# ── Computed cache (regeneratable from notebooks) ────────────────
cache/

# ── Large binary / archive files ─────────────────────────────────
*.parquet
*.gt
*.zip
*.rar
*.shp
*.dbf
*.shx
*.prj
*.cpg

# ── PDFs (copyright) ─────────────────────────────────────────────
*.pdf

# ── Python ───────────────────────────────────────────────────────
__pycache__/
.ipynb_checkpoints/
*.pyc
.env
.env.local

# ── Node / webapp (handled in webapp/.gitignore too) ─────────────
webapp/node_modules/
webapp/dist/
webapp/.vercel/

# ── Local working notes (not for public repo) ───────────────────
doubao_report.md
comparison/
# Raw AI-assist dumps (cleaned CSVs are committed as the actual inputs)
doubao*.txt
deepseek*.txt
上海市民办学校名单 2024.md
A表*.csv
B表*.csv
数据来源与说明文档.md

# ── Misc ─────────────────────────────────────────────────────────
.DS_Store

# doubao4.csv is the raw download; canonical copy is 上海保障性住房项目2024.csv
doubao4.csv
表格_*.csv
doubao7*.txt
doubao7csv*.csv
doubao8.txt
doubaocsv*.csv

# Trello board exports (deliverable artifacts, not source)
jSNASn1u*
15mc-trello-*.json

# Personal submission reference (holds the Trello invite link — keep private)
SUBMISSION.md
